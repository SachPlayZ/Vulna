import { SiteNav } from '../../components/site-nav';

export default function ReviewerPage() {
  return <main id="main-content" className="app-shell"><SiteNav /><section className="page-heading"><p className="eyebrow">Reviewer workspace</p><h1>Verify before you decrypt</h1><p>Only a connected, authorized reviewer can open a stored envelope. The inbox deliberately contains no plaintext preview.</p></section><section className="reviewer-empty"><span>0</span><h2>No verified envelopes in this browser</h2><p>Connect the configured reviewer context, fetch public submission state, verify artifact and envelope hashes, then decrypt locally. Never render active attachments.</p><div className="checklist"><p>□ Verify indexed commitment</p><p>□ Verify ciphertext hash</p><p>□ Decrypt in browser memory</p><p>□ Recompute commitment</p></div></section><aside className="settlement-note"><strong>Settlement boundary</strong><p>PAID records a researcher receipt acknowledgment after a separate transparent wallet transfer. It is not escrow, shielding, or proof of payment.</p></aside></main>;
}
